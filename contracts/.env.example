# Sepolia deployment. The deployer key is a throwaway used only to deploy the
# registry to testnet — it holds no mainnet funds and signs nothing else.
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
DEPLOYER_PRIVATE_KEY=
# Optional, for `forge verify-contract` on Etherscan.
ETHERSCAN_API_KEY=
