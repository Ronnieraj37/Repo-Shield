import { Deck } from "./deck";

export const metadata = {
  title: "RepoShield — The Idea",
  description: "The problem, the solution, and how RepoShield uses Chainlink CRE and The Graph.",
};

export default function DemoPage() {
  return <Deck />;
}
